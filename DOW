Imports System.IO
Imports ExcelDataReader

Using stream As FileStream = File.Open("C:\Path\YourFile.xlsb", FileMode.Open, FileAccess.Read)
    Using reader As IExcelDataReader = ExcelReaderFactory.CreateBinaryReader(stream)
        Dim result As DataSet = reader.AsDataSet()
        dataTableResult = result.Tables(0) ' First sheet
    End Using
End Using
